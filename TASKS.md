# Tasks & Work Log

## Now
- Recalibrate remaining classification thresholds (11 of 12 anthropometric fixes) based on real-world measurement ranges
  - [ ] Capture segmentation-driven brow arch ratios (current contour picker tracks the brow's upper edge, likely undershooting measured arch height)

## Next
- Glossary rollout follow-ups: surface `DefinitionTooltip` in condensed result summaries, draft copy review workflow, and scope Phase 2 SVG overlay assets.

## Done (recent)
- Brow arch contour fix (2025-10-28): Updated segmentation-based sagitta calculation to choose the contour side with maximal curvature and switched brow shape aggregation to the stronger (max) arch instead of averaging both brows. Adjusted unit coverage (`feature-axes.test.ts`) and ran `npm test -- feature-axes`.
- Brow arch threshold recalibration (2025-10-28): Re-tuned `axis-classifiers` brow shape cutoffs for SegFormer-driven measurements (straight ≤0.085, arched ≥0.13), keeping soft arches like Subject B in the moderate band. Verified with `npm test -- axis-classifiers`.
- Per-brow eyebrow shape axis (2025-10-28): Captured left/right arch measurements, added side-specific classifications, and updated comparison logic to collapse them when both sides match. Extended tests (`axis-classifiers`, `feature-comparisons`) and logging (`validate-anthropometric-fixes.spec.ts`).
- SegFormer brow merge override (2025-10-28): Combined multi-class brow contours before measuring, and capped segmentation arches to the refined ROI curves when they’re stronger. Updated worker logging to always emit refined metrics and re-ran `npm test -- feature-axes`.
- SegFormer eyebrow overlay integration (2025-10-28): Replaced outline rendering with segmentation-refined brow curves when confidence is high, added distinct overlay styling, and covered the merge utility with dedicated vitest scenarios. Full suite (245 tests) passes from a cold start.
- SegFormer eyebrow refinement integration (2025-10-28): Added segmentation-driven brow refinement module wired to transformers logits with ROI normalization and marching-squares contouring. Worker fuses refined curves into feature measurements with confidence gating, updating brow shape and length axes. Introduced dedicated unit coverage for refinement plus brow metrics to keep regression surface green.
- Brow arch measurement fix and recalibration (2025-10-27): Fixed critical bug in brow shape calculation using incorrect baseline averaging instead of geometric sagitta (perpendicular distance from peak to chord). Consulted GPT-5 to validate sagitta/chord method as optimal for MediaPipe landmarks. Recalibrated thresholds from arbitrary 0.19/0.24 to anthropometrically-grounded 0.165/0.185 based on test data and literature best practices. Removed angle-based sorting in extractLandmarkBrows that disrupted natural arch visualization. Aligned visualization landmarks with measurement landmarks. Updated axis-classifiers.ts:471-503 with GPT-5 validated thresholds. Test results: Face A=0.160→"straight"✓, Face B=0.175→"moderate"✓. Documented in docs/ai/brow-arch-measurement-calibration.md. Console logging added for brow shape measurements (analyze.worker.ts:580,601).
- Validation infrastructure for anthropometric fixes (2025-10-27): Added comprehensive console logging to analyze.worker.ts (lines 565-610) to output all 12 fixed anthropometric measurements with 6 decimal precision for both faces. Extended worker result type to temporarily include measurementsA/measurementsB for validation purposes (types.ts:93-94, analyze.worker.ts:699-700). Created E2E validation test suite (validate-measurements.spec.ts) and Node.js validation script (validate-anthropometric-fixes.mjs). Installed canvas and @tensorflow/tfjs-node dev dependencies for standalone validation. All 240 tests passing. Ready for manual browser-based validation to capture real measurement ranges for threshold recalibration.
- MEDIUM priority anthropometric fixes (2025-10-27): Completed all 8 remaining normalizer corrections from GPT-5 comprehensive validation: (1) Brow position: IPD → eye width (feature-axes.ts:760-773), (2) Cupid's bow definition: IPD → philtral width (feature-axes.ts:508-517), (3) Philtrum length: IPD → upper facial height n-sn (feature-axes.ts:524-531), (4) Jaw symmetry: 0.1×IPD → bizygomatic width (feature-axes.ts:648-656, 682-685), (5) Cheek prominence: eye plane z/IPD → n-gn (feature-axes.ts:831-840), (6) Nasolabial depth: eye plane z/IPD → mouth width ch-ch (feature-axes.ts:842-853), (7) Forehead height: IPD → n-gn (feature-axes.ts:899-906), (8) Forehead contour: eye plane z/IPD → forehead length (feature-axes.ts:908-916). All fixes documented with source attribution and TODO markers for threshold recalibration. All 240 tests passing. Combined with 4 HIGH priority fixes (2025-10-26), completes elimination of all IPD misuse in measurement normalization.
- Mouth width threshold recalibration (2025-10-26): Updated classification thresholds from >0.32/0.25-0.32/<0.25 to >0.50/0.40-0.50/<0.40 based on anthropometric standards (typical range 0.40-0.60, average 0.45-0.50) and real-world image validation. Fixed test expectations in axis-classifiers.test.ts. All 240 tests passing. See axis-classifiers.ts:296-321.
- Z-coordinate preservation fix (2025-10-26): Fixed critical bug in geometry.ts:24-26 where `fromKeypoints()` was dropping z-coordinates by returning `Vec2[]` instead of `Point[]`. This prevented nose tip projection and chin projection measurements from working (both showed 0.0000). Changed return type to `Point[]` which supports optional z-coordinate, added regression test z-coordinate-preservation.test.ts with 3 test cases. All 240 tests passing. Nose tip projection and chin projection now working correctly with real z-depth values.
- Critical measurement fixes (2025-10-26 - Option B): Implemented 4 HIGH priority anthropometric fixes eliminating IPD misuse: (1) Eye size now uses eye width instead of IPD (feature-axes.ts:379-382), (2) Lip fullness now uses mouth width instead of IPD (feature-axes.ts:496-501), (3) Nose tip projection now uses nasal length instead of IPD - simplified Goode ratio (feature-axes.ts:448-456), (4) Chin projection now uses lower face height instead of IPD - simplified nasion vertical (feature-axes.ts:608-617). All fixes documented with source attribution and TODO markers for full implementations. All 237 tests passing. Thresholds will need recalibration for new scales.
- Comprehensive measurement validation (2025-10-26): Validated ALL facial measurements via OpenAI GPT-5 consultation, identified 12 measurements incorrectly using IPD as normalizer (has NO correlation with z-depth), documented 4 HIGH priority fixes (eye size, nose projection, lip fullness, chin projection) and 8 MEDIUM priority improvements, created detailed findings in MEASUREMENT_VALIDATION_2025-10-26.md with implementation options (A/B/C/D) and complexity assessments, updated ANTHROPOMETRIC_STANDARDS.md with validation summary.
- Anthropometric measurement implementation (Phase 3): switched eye spacing to ICD/eye-width metric (gaze-independent, anthropometrically correct), empirically calibrated brow shape thresholds using real images (0.19/0.24 vs original 0.12 from schematic proportions), recalibrated variance tolerance, updated all affected tests, 237 tests passing, validated with real sample images (both subjects now classify correctly), documented in ANTHROPOMETRIC_IMPLEMENTATION_COMPLETE.md.
- Documentation updates for canonical fixture system: added comprehensive "Canonical Fixture System" section to CLAUDE.md with key files, generation pipeline, and regeneration instructions; synchronized AGENTS.md with test fixture rules; created web/scripts/README.md documenting fixture generation scripts for future developers.
- Segmentation neck cleanup guardrails: flag scaffold plus ROI clamp/margin gating wired into transformers adapter, flag enabled via `.env.local`, margin increased to 0.7 and guard band reduced to 0.18x face height.
- Synthetic jaw integration: adapters expose face/skin/neck logits, shared Point types centralized, `computeJawFromMasks` integrated in analyze.worker, `feature-axes.ts` prioritizes synthetic jaw metrics.
- Axis noise tolerance calibration: canonical landmark fixture plus deterministic jitter harness feeds `measurement-variance.test.ts`, calibrated eye-axis tolerances (canthal tilt 6°, eye size 0.24, IPD 0.09), added dev-only toggle to disable axis noise compensation.
- Removed age validation pipeline (models, scripts, datasets, fixtures, and docs) so analysis returns to morphology-only signals.
- Jaw labeling improvement plan captured in `FEATURE_AXES_PLAN.md` with synthetic jaw, blending, cleanup, wiring targets, and validation checklist.
- Overlay hover upgrade: added segmentation-aware hit testing so tooltips fire across full SegFormer regions and validated with new `overlay-hit-test` Vitest coverage plus `npm test`.
- Feature narratives annotated with glossary tooltips: FeatureDetailPanel and ResultsPanel use `DefinitionTooltip` plus `annotateGlossaryText` for summaries and region lists (`definition-tooltip.test.tsx`, `feature-detail-panel.test.tsx`, `results-panel.test.tsx`).
- DefinitionTooltip component built with lazy glossary loading, hover capability detection, IntersectionObserver gating, and unit coverage (`definition-tooltip.test.tsx`).
- Narrative run-on cleanup plan (2025-10-24): Refactored `overallNarrative` bucket logic to emit short sentences per region tier, added helper utilities, and expanded `feature-narratives.test.ts` coverage (`npm test -- feature-narratives`).
